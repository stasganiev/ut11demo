# TODO: Конструктор печатной формы «Счёт на оплату»

Статусы: `[ ]` — не начато, `[~]` — в работе, `[x]` — выполнено

---

## Этап 1. Метаданные

- [x] 1.1. Создать обработку `ПечатьСчетНаОплатуКонструктор` (.mdo)
- [x] 1.2. Создать макет `ПФ_MXL_СчетНаОплатуКонструктор` с именованными областями:
  - `ШапкаПолная`, `ШапкаКраткая`
  - `РеквизитыПокупателя`
  - `ТаблицаТоваровДетальная_Шапка`, `ТаблицаТоваровДетальная_Строка`, `ТаблицаТоваровДетальная_Подвал`
  - `ТаблицаТоваровИтоги`
  - `ЭтапыОплаты_Шапка`, `ЭтапыОплаты_Строка`
  - `ПодписиПолные`, `ПодписиМенеджер`
  - `ПримечаниеКДокументу`
- [~] 1.3. Создать форму `НастройкаБлоков` (таблица блоков с флажками, кнопки вверх/вниз, кнопка «Сформировать») — каркас создан, элементы управления будут на этапе 3
- [x] 1.4. Создать общий модуль `ПечатьСчетНаОплатуКонструкторКлиент` (клиент, вызов сервера)
  - Функция `ОткрытьКонструктор(ПараметрыПечати) Экспорт` — обработчик команды печати (обязательно функция — вызывается через `Вычислить()`, см. УправлениеПечатьюСлужебныйКлиент:128-129)
  - Входной параметр `ПараметрыПечати` — структура `УправлениеПечатьюКлиент.ОписаниеПараметровПечати()`, из неё берём `ОбъектыПечати`
  - Открывает форму `НастройкаБлоков` с передачей `ПараметрыПечати.ОбъектыПечати`
  - Возвращает `Неопределено`
- [x] 1.5. Создать регистр сведений `НастройкиКонструктораПечати` (.mdo)
  - Измерения: Пользователь (СправочникСсылка.Пользователи), ИмяПечатнойФормы (Строка 150)
  - Ресурс: НастройкиБлоков (ХранилищеЗначения)
- [x] 1.6. Добавить обработку, общий модуль и регистр в подсистему Продажи

## Этап 2. Модуль менеджера обработки

- [ ] 2.1. Реализовать `Печать()` — точка входа по паттерну БСП
  - Проверка `УправлениеПечатью.НужноПечататьМакет()`
  - Вывод через `УправлениеПечатью.ВывестиТабличныйДокументВКоллекцию()`
- [ ] 2.2. Реализовать `ПолучитьДанныеДляПечати()` — запрос данных документа:
  - Шапка (реквизиты организации, контрагента)
  - Товары (номенклатура, количество, цена, сумма, НДС)
  - Итоги (суммы, НДС)
  - Этапы оплаты
  - Подписанты
  - Комментарий
- [ ] 2.3. Реализовать `СформироватьТабличныйДокументПоБлокам(МассивОбъектов, НастройкиБлоков)` — сборка табличного документа из выбранных блоков в заданном порядке:
  - Соответствие «идентификатор блока → массив областей макета» (см. таблицу в architecture.md)
  - Цикл по массиву активных блоков
  - Для каждого: `Макет.ПолучитьОбласть()` → заполнение параметров → `ТабличныйДокумент.Вывести()`

## Этап 3. Форма настройки блоков

- [ ] 3.1. Серверная инициализация:
  - Загрузка сохранённых настроек из регистра `НастройкиКонструктораПечати`
  - Если нет сохранённых — подставить значения по умолчанию
  - Заполнение таблицы блоков (идентификатор, представление, флаг включения, порядок, группа)
- [ ] 3.2. Клиентские обработчики:
  - Включение/выключение блока (флажок)
  - Взаимоисключение: при включении блока из группы — автовыключение другого блока той же группы
  - Кнопки «Вверх» / «Вниз» — перемещение блока в списке
- [ ] 3.3. Сохранение/загрузка пресетов:
  - Кнопка «Сохранить настройки» → запись в регистр
  - Автозагрузка при открытии формы
- [ ] 3.4. Кнопка «Сформировать»:
  - Сбор массива активных блоков с порядком
  - Вызов сервера → `СформироватьТабличныйДокументПоБлокам()`
  - Открытие результата в типовом предпросмотре `УправлениеПечатью`

## Этап 4. Интеграция

- [ ] 4.1. Добавить команду печати в `Документ.ЗаказКлиента.ДобавитьКомандыПечати()` (ManagerModule.bsl:3271)
  - Свойство `Обработчик` = `"ПечатьСчетНаОплатуКонструкторКлиент.ОткрытьКонструктор"`
  - Свойство `МенеджерПечати` = `""`
  - Свойство `Идентификатор` = `"СчетНаОплатуКонструктор"`
  - Вставка перед вызовом `ЗаказКлиентаЛокализация.ДобавитьКомандыПечати` (строка 3447)
- [ ] 4.2. Создать роль на чтение/запись регистра `НастройкиКонструктораПечати`
- [ ] 4.3. Назначить роль профилям доступа менеджеров

## Этап 5. Проверка

- [ ] 5.1. Печать, сохранение в PDF, предпросмотр через типовой механизм
- [ ] 5.2. Сохранение и загрузка пресетов (новый пользователь, повторное открытие)
- [ ] 5.3. Взаимоисключающие блоки (нельзя включить оба варианта группы)
- [ ] 5.4. Крайние случаи: только итоги без таблицы, только краткая шапка
- [ ] 5.5. Документы с разным составом данных (с/без НДС, с/без этапов оплаты, пустой комментарий)
- [ ] 5.6. Проверка разделения клиент/сервер (директивы компиляции)
- [ ] 5.7. Проверка прав доступа (менеджер видит и использует форму, обычный пользователь — нет)
- [ ] 5.8. Проверка, что команда «Счёт на оплату (конструктор)...» появляется в меню печати ЗаказКлиента
