#Область СлужебныйПрограммныйИнтерфейс

// Преобразовывает текстовое представление статуса кода маркировки МОТП в значение перечисления.
//
// Параметры:
//  ЗначениеПоиска - Строка - значение для перекодировки
// 
// Возвращаемое значение:
//  ПеречислениеСсылка.СтатусыКодовМаркировкиСУЗ - статус кода маркировки.
//
Функция СтатусКодаМаркировки(Знач ЗначениеПоиска) Экспорт
	
	ЗначениеПоиска = ВРег(ЗначениеПоиска);
	
	Если ЗначениеПоиска = "GENERATED" Тогда
		Возврат Перечисления.СтатусыКодовМаркировкиСУЗ.Сгенерирован;
	ИначеЕсли ЗначениеПоиска = "UTILISED" Тогда
		Возврат Перечисления.СтатусыКодовМаркировкиСУЗ.Использован;
	ИначеЕсли ЗначениеПоиска = "DROPPED_OUT" Тогда
		Возврат Перечисления.СтатусыКодовМаркировкиСУЗ.Отбракован;
	ИначеЕсли ЗначениеПоиска = "PACKED" Тогда
		Возврат Перечисления.СтатусыКодовМаркировкиСУЗ.Упакован;
	КонецЕсли;
	
	ВызватьИсключение
		СтрШаблон(
			НСтр("ru = 'Неизвестный статус кода маркировки: %1'"),
			ЗначениеПоиска);
	
КонецФункции

// Преобразовывает текстовое представление статуса обработки отчёта СУЗ в значение перечисления.
//
// Параметры:
//  ЗначениеПоиска - Строка - значение для перекодировки
// 
// Возвращаемое значение:
//  ПеречислениеСсылка.СтатусыОбработкиСообщенийИСМП - статус обработки отчета.
//
Функция СтатусОбработкиОтчета(Знач ЗначениеПоиска) Экспорт
	
	ЗначениеПоиска = ВРег(ЗначениеПоиска);
	
	Если ЗначениеПоиска = "DRAFT" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаПринята;
	ИначеЕсли ЗначениеПоиска = "PENDING" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаОбрабатывается;
	ИначеЕсли ЗначениеПоиска = "READY_TO_SEND" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаОбрабатывается;
	ИначеЕсли ЗначениеПоиска = "REJECTED" Или  ЗначениеПоиска = "CHECKED_NOT_OK" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаОтклонена;
	ИначеЕсли ЗначениеПоиска = "SENT" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаВыполнена;
	ИначеЕсли ЗначениеПоиска = "CHECK" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаОбрабатывается;
	ИначеЕсли ЗначениеПоиска = "PROCESSED" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаВыполнена;
	ИначеЕсли ЗначениеПоиска = "CHECKED_OK" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаВыполнена;
	КонецЕсли;
	
	ВызватьИсключение
		СтрШаблон(
			НСтр("ru = 'Неизвестный статус отчета СУЗ: %1'"),
			ЗначениеПоиска);

КонецФункции

// Преобразовывает текстовое представление статуса обработки отчёта о нанесении СУЗ в значение перечисления.
//
// Параметры:
//  ЗначениеПоиска - Строка - значение для перекодировки
//
// Возвращаемое значение:
//  ПеречислениеСсылка.СтатусыОбработкиСообщенийИСМП - статус обработки отчета.
//
Функция СтатусОбработкиОтчетаОНанесении(Знач ЗначениеПоиска) Экспорт

	ЗначениеПоиска = ВРег(ЗначениеПоиска);

	Если ЗначениеПоиска = "DRAFT" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаПринята;
	ИначеЕсли ЗначениеПоиска = "PENDING" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаОбрабатывается;
	ИначеЕсли ЗначениеПоиска = "READY_TO_SEND" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаОбрабатывается;
	ИначеЕсли ЗначениеПоиска = "REJECTED" Или  ЗначениеПоиска = "CHECKED_NOT_OK" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаОтклонена;
	ИначеЕсли ЗначениеПоиска = "SENT" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаВыполнена;
	ИначеЕсли ЗначениеПоиска = "CHECK" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаОбрабатывается;
	ИначеЕсли ЗначениеПоиска = "PROCESSED" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаВыполнена;
	ИначеЕсли ЗначениеПоиска = "CHECKED_OK" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаВыполнена;
	ИначеЕсли ЗначениеПоиска = "SUCCESS" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаВыполнена;
	ИначеЕсли ЗначениеПоиска = "FAILED" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаОтклонена;
	ИначеЕсли ЗначениеПоиска = "SUCCESSPARTIALLY" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаОбрабатывается;
	КонецЕсли;

	ВызватьИсключение
		СтрШаблон(
			НСтр("ru = 'Неизвестный статус отчета о нанесении СУЗ: %1'"),
			ЗначениеПоиска);

КонецФункции

// Преобразовывает текстовое представление статуса кода маркировки МОТП в значение перечисления.
//
// Параметры:
//  ЗначениеПоиска - Строка - значение для перекодировки
// 
// Возвращаемое значение:
//  ПеречислениеСсылка.СтатусыОбработкиСообщенийИСМП - статус обработки заявки.
//
Функция СтатусОбработкиЗапросаНаПолучениеИдентификатораСоединения(Знач ЗначениеПоиска) Экспорт
	
	ЗначениеПоиска = ВРег(ЗначениеПоиска);
	
	Если ЗначениеПоиска = "SUCCESS" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаВыполнена;
	ИначеЕсли ЗначениеПоиска = "REJECTED" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаОтклонена;
	КонецЕсли;
	
	ВызватьИсключение
		СтрШаблон(
			НСтр("ru = 'Неизвестный статус запрос на получение идентификатора соединения: %1'"),
			ЗначениеПоиска);
	
КонецФункции

Функция РезультатОбработкиОтчетаСУЗ(Знач ЗначениеПоиска) Экспорт
	
	ЗначениеПоиска = ВРег(ЗначениеПоиска);
	
	Если ЗначениеПоиска = "SUCCESS" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаВыполнена;
	ИначеЕсли ЗначениеПоиска = "FAILED" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.Ошибка;
	ИначеЕсли ЗначениеПоиска = "IN_PROGRESS" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаОбрабатывается;
	ИначеЕсли ЗначениеПоиска = "PARTIALLY" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаОбработанаЧастично;
	ИначеЕсли ЗначениеПоиска = "ACCEPTED" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаВыполнена;
	ИначеЕсли ЗначениеПоиска = "REJECTED" Тогда
		Возврат Перечисления.СтатусыОбработкиСообщенийИСМП.ЗаявкаОтклонена;
	КонецЕсли;
	
	ВызватьИсключение
		СтрШаблон(
			НСтр("ru = 'Неизвестный результат обработки документа СУЗ: %1'"),
			ЗначениеПоиска);
	
КонецФункции

	
// Преобразовывает вид операции ИС МП в вид отчета об использовании кодов маркировки.
//
// Параметры:
//  ЗначениеПоиска - ПеречислениеСсылка.ВидыОперацийИСМП - значение для перекодировки
// 
// Возвращаемое значение:
//  Строка - Вид отчета об использовании кодов маркировки.
//
Функция ВидОтчетаОбИспользованииКодовМаркировки(Знач ЗначениеПоиска) Экспорт
	
	Если ЗначениеПоиска = Перечисления.ВидыОперацийИСМП.УдалитьОтчетОПередачеКМНаПроизводственнуюЛинию Тогда
		Возврат "USED_FOR_PRODUCTION";
	ИначеЕсли ЗначениеПоиска = Перечисления.ВидыОперацийИСМП.УдалитьОтчетОПередачеКМНаПринтер Тогда
		Возврат "SENT_TO_PRINTER";
	ИначеЕсли ЗначениеПоиска = Перечисления.ВидыОперацийИСМП.УдалитьОтчетОПечатиКМ Тогда
		Возврат "PRINTED";
	ИначеЕсли ЗначениеПоиска = Перечисления.ВидыОперацийИСМП.УдалитьОтчетОПотереРаспечатанныхКМ Тогда
		Возврат "PRINTER_LOST";
	ИначеЕсли ЗначениеПоиска = Перечисления.ВидыОперацийИСМП.ОтчетОВерификацииНанесенныхКМ Тогда
		Возврат "VERIFIED";
	КонецЕсли;
	
	ВызватьИсключение
		СтрШаблон(
			НСтр("ru = 'Неизвестный вид отчета об использовании КМ СУЗ: %1'"),
			ЗначениеПоиска);
	
КонецФункции

// Преобразовывает вид операции ИС МП в вид отчета об использовании кодов маркировки.
//
// Параметры:
//  ЗначениеПоиска - ПеречислениеСсылка.ВидыОперацийИСМП - значение для перекодировки
// 
// Возвращаемое значение:
//  Строка - Вид отчета об использовании кодов маркировки.
//
Функция ПричинаСписанияКМ(Знач ЗначениеПоиска) Экспорт
	
	Если ЗначениеПоиска = Перечисления.ПричиныСписанияКодовМаркировкиИСМП.Испорчен Тогда
		Возврат "DEFECT";
	ИначеЕсли ЗначениеПоиска = Перечисления.ПричиныСписанияКодовМаркировкиИСМП.ИстечениеСрока Тогда
		Возврат "EXPIRY";
	ИначеЕсли ЗначениеПоиска = Перечисления.ПричиныСписанияКодовМаркировкиИСМП.Уничтожен Тогда
		Возврат "DESTRUCTION";
	ИначеЕсли ЗначениеПоиска = Перечисления.ПричиныСписанияКодовМаркировкиИСМП.Утерян Тогда
		Возврат "DAMAGE_LOSS";
	ИначеЕсли ЗначениеПоиска = Перечисления.ПричиныСписанияКодовМаркировкиИСМП.Прочее Тогда
		Возврат "OTHER";	
	ИначеЕсли ЗначениеПоиска = Перечисления.ПричиныСписанияКодовМаркировкиИСМП.ЛабораторныеОбразцы Тогда
		Возврат "QA_SAMPLES";	
	ИначеЕсли ЗначениеПоиска = Перечисления.ПричиныСписанияКодовМаркировкиИСМП.ОтзывСРынка Тогда
		Возврат "PRODUCT_RECALL";
	ИначеЕсли ЗначениеПоиска = Перечисления.ПричиныСписанияКодовМаркировкиИСМП.Рекламации Тогда
		Возврат "COMPLAINTS";	
	ИначеЕсли ЗначениеПоиска = Перечисления.ПричиныСписанияКодовМаркировкиИСМП.ТестированиеПродукта Тогда
		Возврат "PRODUCT_TESTING";	
	ИначеЕсли ЗначениеПоиска = Перечисления.ПричиныСписанияКодовМаркировкиИСМП.ДемонстрационныеОбразцы Тогда
		Возврат "DEMO_SAMPLES";	
	ИначеЕсли ЗначениеПоиска = Перечисления.ПричиныСписанияКодовМаркировкиИСМП.ЛиквидацияПредприятия Тогда
		Возврат "LIQUIDATION";	
	ИначеЕсли ЗначениеПоиска = Перечисления.ПричиныСписанияКодовМаркировкиИСМП.КонфискацияТоваров Тогда
		Возврат "CONFISCATION";
	ИначеЕсли ЗначениеПоиска = Перечисления.ПричиныСписанияКодовМаркировкиИСМП.ИспользованиеДляСобственныхНуждСУплатойАкциза Тогда
		Возврат "OWN_USE_EXCISE";
	КонецЕсли;
	
	ВызватьИсключение
		СтрШаблон(
			НСтр("ru = 'Неизвестная причина списания КМ СУЗ: %1'"),
			ЗначениеПоиска);

КонецФункции

// Преобразовывает причину списания ИС МП в значение справочника "Причина выбраковки знака" для КИЗ.
//
// Параметры:
//  ЗначениеПоиска - ПеречислениеСсылка.ПричиныСписанияКодовМаркировкиИСМП - значение для перекодировки
//
// Возвращаемое значение:
//  Строка - причина выбраковки знака.
//
Функция ПричинаСписанияКИЗ(Знач ЗначениеПоиска) Экспорт

	Если ЗначениеПоиска = Перечисления.ПричиныСписанияКодовМаркировкиИСМП.Испорчен Тогда
		Возврат "CORRUPTED";
	ИначеЕсли ЗначениеПоиска = Перечисления.ПричиныСписанияКодовМаркировкиИСМП.Утерян Тогда
		Возврат "LOST";
	КонецЕсли;

	ВызватьИсключение
		СтрШаблон(
			НСтр("ru = 'Неизвестная причина списания КИЗ СУЗ: %1'"),
			ЗначениеПоиска);

КонецФункции

// Преобразовывает текстовое представление статуса кода маркировки МОТП в значение перечисления.
//
// Параметры:
//  ЗначениеПоиска - Строка - значение для перекодировки
// 
// Возвращаемое значение:
//  ПеречислениеСсылка.СтатусыКодовМаркировкиСУЗ - статус кода маркировки.
//
Функция СтатусБизнесЗаказа(Знач ЗначениеПоиска) Экспорт
	
	ЗначениеПоиска = ВРег(ЗначениеПоиска);
	
	Если ЗначениеПоиска = "CREATED" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.СУЗСоздан;
	ИначеЕсли ЗначениеПоиска = "PENDING" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.СУЗПринятКОбработке;
	ИначеЕсли ЗначениеПоиска = "APPROVED" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.СУЗПодтвержденИСМП;
	ИначеЕсли ЗначениеПоиска = "READY" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.СУЗПринятКПроизводству;
	ИначеЕсли ЗначениеПоиска = "CLOSED" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.СУЗЗакрыт;
	ИначеЕсли ЗначениеПоиска = "DECLINED" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.Ошибка;
	ИначеЕсли ЗначениеПоиска = "EXPIRED" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.СУЗАннулирован;
	ИначеЕсли ЗначениеПоиска = "CANCELED" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.Аннулирован;
	ИначеЕсли ЗначениеПоиска = "UNDER_CONSIDERATION" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.СУЗНаРассмотренииЭмитента;
	ИначеЕсли ЗначениеПоиска = "SUSPENDED" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.СУЗПриостановлено;
	ИначеЕсли ЗначениеПоиска = "ACCOUNT" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.СУЗФормированиеСчета;
	ИначеЕсли ЗначениеПоиска = "WAITING_PAYMENT" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.СУЗОжиданиеОплаты;
	ИначеЕсли ЗначениеПоиска = "TRANSFERRED_PRODUCTION" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.СУЗПереданоВПроизводство;
	ИначеЕсли ЗначениеПоиска = "TRANSFERRED" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.СУЗИзготовленоИПереданоГрузоперевозчику;
	ИначеЕсли ЗначениеПоиска = "AWAITING_PICKUP" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.СУЗИзготовленоОжидаетСамовывоза;
	ИначеЕсли ЗначениеПоиска = "ACTIVE" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.СУЗОтгруженоЗаказчику;
	ИначеЕсли ЗначениеПоиска = "RETURNED" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.СУЗВозвращено;
	ИначеЕсли ЗначениеПоиска = "DESTROYED" Тогда
		Возврат Перечисления.СтатусыОбработкиЗаказовНаЭмиссиюКодовМаркировкиИСМП.СУЗУничтожено;
	КонецЕсли;
	
	ВызватьИсключение
		СтрШаблон(
			НСтр("ru = 'Неизвестный статус отчета СУЗ: %1'"),
			ЗначениеПоиска);
	
КонецФункции

// Преобразовывает текстовое представление статуса кода маркировки МОТП в значение перечисления.
//
// Параметры:
//  ЗначениеПоиска - Строка - значение для перекодировки
// 
// Возвращаемое значение:
//  ПеречислениеСсылка.СтатусыКодовМаркировкиСУЗ - статус кода маркировки.
//
Функция СтатусПулаКодовМаркировки_V1(Знач ЗначениеПоиска) Экспорт
	
	ЗначениеПоиска = ВРег(ЗначениеПоиска);
	
	Если ЗначениеПоиска = "PENDING" Тогда
		Возврат Перечисления.СтатусыПулаКодовМаркировкиСУЗ.Обрабатывается;
	ИначеЕсли ЗначениеПоиска = "READY" Тогда
		Возврат Перечисления.СтатусыПулаКодовМаркировкиСУЗ.Готов;
	ИначеЕсли ЗначениеПоиска = "REJECTED" Тогда
		Возврат Перечисления.СтатусыПулаКодовМаркировкиСУЗ.Ошибка;
	ИначеЕсли ЗначениеПоиска = "CLOSED" Тогда
		Возврат Перечисления.СтатусыПулаКодовМаркировкиСУЗ.Закрыт;
	КонецЕсли;
	
	ВызватьИсключение
		СтрШаблон(
			НСтр("ru = 'Неизвестный статус пула кода маркировки: %1'"),
			ЗначениеПоиска);
	
КонецФункции

// Преобразовывает текстовое представление статуса кода маркировки МОТП в значение перечисления.
//
// Параметры:
//  ЗначениеПоиска - Строка - значение для перекодировки
// 
// Возвращаемое значение:
//  ПеречислениеСсылка.СтатусыКодовМаркировкиСУЗ - статус кода маркировки.
//
Функция СтатусПулаКодовМаркировки_V2(Знач ЗначениеПоиска) Экспорт
	
	ЗначениеПоиска = ВРег(ЗначениеПоиска);
	
	Если ЗначениеПоиска = "PENDING" Тогда
		Возврат Перечисления.СтатусыПулаКодовМаркировкиСУЗ.Обрабатывается;
	ИначеЕсли ЗначениеПоиска = "ACTIVE" Тогда
		Возврат Перечисления.СтатусыПулаКодовМаркировкиСУЗ.Готов;
	ИначеЕсли ЗначениеПоиска = "CLOSED" Тогда
		Возврат Перечисления.СтатусыПулаКодовМаркировкиСУЗ.Закрыт;
	ИначеЕсли ЗначениеПоиска = "REJECTED" Тогда
		Возврат Перечисления.СтатусыПулаКодовМаркировкиСУЗ.Отклонен;
	ИначеЕсли ЗначениеПоиска = "EXHAUSTED" Тогда
		Возврат Перечисления.СтатусыПулаКодовМаркировкиСУЗ.Закрыт;
	ИначеЕсли ЗначениеПоиска = "EXPIRED" Тогда
		Возврат Перечисления.СтатусыПулаКодовМаркировкиСУЗ.Отклонен;
	КонецЕсли;
	
	ВызватьИсключение
		СтрШаблон(
			НСтр("ru = 'Неизвестный статус пула кода маркировки: %1'"),
			ЗначениеПоиска);
	
КонецФункции

// Перекодирует способ выпуска товаров в для обмена с СУЗ ИС МП.
//
// Параметры:
//  ЗначениеПоиска - Строка, ПеречислениеСсылка.СпособыВводаВОборотСУЗ - значение для перекодировки.
//  Назначение     - Строка - СУЗ, ИСМП или ОСТАТКИ
//
// Возвращаемое значение:
//  Строка, ПеречислениеСсылка.СпособыВводаВОборотСУЗ - Результат перекодирования.
//
Функция СпособВыпускаВОборот(Знач ЗначениеПоиска, Назначение = "СУЗ") Экспорт
	
	Если ЗначениеПоиска = "PRODUCTION" Или ЗначениеПоиска = "LOCAL" Или ЗначениеПоиска = "PRODUCED_IN_RF" Тогда
		Результат = Перечисления.СпособыВводаВОборотСУЗ.Производство;
	ИначеЕсли ЗначениеПоиска = "IMPORT" Или ЗначениеПоиска = "FOREIGN" Или ЗначениеПоиска = "IMPORTED_INTO_RF" Тогда
		Результат = Перечисления.СпособыВводаВОборотСУЗ.Импорт;
	ИначеЕсли ЗначениеПоиска = "REMARK" Тогда
		Результат = Перечисления.СпособыВводаВОборотСУЗ.Перемаркировка;
	ИначеЕсли ЗначениеПоиска = "REMAINS" Тогда
		Результат = Перечисления.СпособыВводаВОборотСУЗ.МаркировкаОстатков;
	ИначеЕсли ЗначениеПоиска = "CROSSBORDER" Тогда
		Результат = Перечисления.СпособыВводаВОборотСУЗ.ТрансграничнаяТорговля;
	ИначеЕсли ЗначениеПоиска = "COMMISSION" Тогда
		Результат = Перечисления.СпособыВводаВОборотСУЗ.Комиссия;
		
	ИначеЕсли ЗначениеПоиска = Перечисления.СпособыВводаВОборотСУЗ.Производство И Назначение = "СУЗ" Тогда
		Результат = "PRODUCTION";
	ИначеЕсли ЗначениеПоиска = Перечисления.СпособыВводаВОборотСУЗ.Производство И Назначение = "ИСМП" Тогда
		Результат = "LOCAL";
	ИначеЕсли ЗначениеПоиска = Перечисления.СпособыВводаВОборотСУЗ.Производство И Назначение = "ОСТАТКИ" Тогда
		Результат = "PRODUCED_IN_RF";
	ИначеЕсли ЗначениеПоиска = Перечисления.СпособыВводаВОборотСУЗ.Импорт И Назначение = "СУЗ" Тогда
		Результат = "IMPORT";
	ИначеЕсли ЗначениеПоиска = Перечисления.СпособыВводаВОборотСУЗ.Импорт И Назначение = "ИСМП" Тогда
		Результат = "FOREIGN";
	ИначеЕсли ЗначениеПоиска = Перечисления.СпособыВводаВОборотСУЗ.Импорт И Назначение = "ОСТАТКИ" Тогда
		Результат = "IMPORTED_INTO_RF";
	ИначеЕсли ЗначениеПоиска = Перечисления.СпособыВводаВОборотСУЗ.Перемаркировка Тогда
		Результат = "REMARK";
	ИначеЕсли ЗначениеПоиска = Перечисления.СпособыВводаВОборотСУЗ.МаркировкаОстатков Тогда
		Результат = "REMAINS";
	ИначеЕсли ЗначениеПоиска = Перечисления.СпособыВводаВОборотСУЗ.ТрансграничнаяТорговля Тогда
		Результат = "CROSSBORDER";
	ИначеЕсли ЗначениеПоиска = Перечисления.СпособыВводаВОборотСУЗ.Комиссия Тогда
		Результат = "COMMISSION";
	Иначе
		ВызватьИсключение
		СтрШаблон(
			НСтр("ru = 'Неизвестный способ выпуска в оборот: %1'"),
			ЗначениеПоиска);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Перекодирует шаблон кода маркировки для обмена с СУЗ ИС МП.
//
// Параметры:
//  ЗначениеПоиска - Строка, ПеречислениеСсылка.ШаблоныКодовМаркировкиСУЗ - значение для перекодировки.
// 
// Возвращаемое значение:
//  Строка, ПеречислениеСсылка.ШаблоныКодовМаркировкиСУЗ - Результат перекодирования.
//
Функция ШаблонКодаМаркировки(Знач ЗначениеПоиска) Экспорт
	
	Если ЗначениеПоиска = 1 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.Обувь;
	ИначеЕсли ЗначениеПоиска = 3 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.БлокТабачныхПачек;
	ИначеЕсли ЗначениеПоиска = 4 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.ТабачнаяПачка;
	ИначеЕсли ЗначениеПоиска = 6 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.МолочнаяПродукцияПодконтрольнаяВЕТИС;
	ИначеЕсли ЗначениеПоиска = 7 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.Шины;
	ИначеЕсли ЗначениеПоиска = 8 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.Фотоаппараты;
	ИначеЕсли ЗначениеПоиска = 10 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.ЛегкаяПромышленность;
	ИначеЕсли ЗначениеПоиска = 9 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.Духи;
	ИначеЕсли ЗначениеПоиска = 11 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.Велосипеды;
	ИначеЕсли ЗначениеПоиска = 12 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.КреслаКоляски;
	ИначеЕсли ЗначениеПоиска = 14 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.АльтернативныйТабакБлок;
	ИначеЕсли ЗначениеПоиска = 15 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.АльтернативныйТабакПачка;
	ИначеЕсли ЗначениеПоиска = 16 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.УпакованнаяВода;
	ИначеЕсли ЗначениеПоиска = 18 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.Пиво;
	ИначеЕсли ЗначениеПоиска = 20 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.МолочнаяПродукцияСерия6СимволовВЕТИС;
	ИначеЕсли ЗначениеПоиска = 21 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.НикотиносодержащаяПродукцияБлок; // Блок и набор имеют одинаковый шаблон
	ИначеЕсли ЗначениеПоиска = 22 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.НикотиносодержащаяПродукцияПачка;
	ИначеЕсли ЗначениеПоиска = 23 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.БАДы;
	ИначеЕсли ЗначениеПоиска = 25 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.Антисептики;
	ИначеЕсли ЗначениеПоиска = 27 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.МорепродуктыСтандартныйКМ;
	ИначеЕсли ЗначениеПоиска = 28 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.БезалкогольноеПиво;
	ИначеЕсли ЗначениеПоиска = 29 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.СоковаяПродукция;
	ИначеЕсли ЗначениеПоиска = 30 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.БАДыСтандартныйКМ;
	ИначеЕсли ЗначениеПоиска = 31 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.АнтисептикиСтандартныйКМ;
	ИначеЕсли ЗначениеПоиска = 38 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.Морепродукты;
	ИначеЕсли ЗначениеПоиска = 26 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.КормаДляЖивотныхСтандартныйКМ;
	ИначеЕсли ЗначениеПоиска = 32 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.Мясо;
	ИначеЕсли ЗначениеПоиска = 34 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.ИгрыИИгрушкиДляДетей;
	ИначеЕсли ЗначениеПоиска = 36 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.РадиоэлектроннаяПродукцияСтандартныйКМ;
	ИначеЕсли ЗначениеПоиска = 37 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.РадиоэлектроннаяПродукция;
	ИначеЕсли ЗначениеПоиска = 39 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.ТитановаяМеталлопродукция;
	ИначеЕсли ЗначениеПоиска = 40 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.РастительныеМасла;
	ИначеЕсли ЗначениеПоиска = 41 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.КормаДляЖивотных;
	ИначеЕсли ЗначениеПоиска = 44 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.ОптоволокноИОптоволоконнаяПродукцияСтандартныйКМ;
	ИначеЕсли ЗначениеПоиска = 45 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.ОптоволокноИОптоволоконнаяПродукция;
	ИначеЕсли ЗначениеПоиска = 46 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.ПарфюмерныеИКосметическиеСредстваИБытоваяХимияСтандартныйКМ;
	ИначеЕсли ЗначениеПоиска = 47 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.ПарфюмерныеИКосметическиеСредстваИБытоваяХимия;
	ИначеЕсли ЗначениеПоиска = 48 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.КонсервированнаяПродукцияСтандартныйКМ;
	ИначеЕсли ЗначениеПоиска = 49 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.КонсервированнаяПродукция;
	ИначеЕсли ЗначениеПоиска = 50 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.ВетеринарныеПрепараты;
	ИначеЕсли ЗначениеПоиска = 51 Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.РастительныеМаслаСтандартныйКМ;

	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.Обувь Тогда
		Результат = 1;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.БлокТабачныхПачек Тогда
		Результат = 3;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ТабачнаяПачка Тогда
		Результат = 4;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.МолочнаяПродукцияПодконтрольнаяВЕТИС
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.МолочнаяПродукцияБезУказанияСроковГодностиВЕТИС
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.СкоропортящаясяМолочнаяПродукцияВЕТИС
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.МолочнаяПродукцияБезВЕТИС Тогда
		Результат = 6;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.Шины Тогда
		Результат = 7;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.Фотоаппараты
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ФотоаппаратыНабор Тогда
		Результат = 8;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.Духи
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ДухиНабор Тогда
		Результат = 9;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ЛегкаяПромышленность
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ЛегкаяПромышленностьНабор Тогда
		Результат = 10;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.Велосипеды Тогда
		Результат = 11;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.КреслаКоляски
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.КреслаКоляскиНабор
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.КреслаКоляскиГрупповаяУпаковка Тогда
		Результат = 12;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.АльтернативныйТабакПачкаGS1 Тогда
		Результат = 14;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.АльтернативныйТабакБлок Тогда
		Результат = 14;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.АльтернативныйТабакПачка Тогда
		Результат = 15;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.УпакованнаяВода
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.УпакованнаяВодаГрупповаяУпаковка Тогда
		Результат = 16;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.Пиво
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ПивоГрупповаяУпаковка Тогда
		Результат = 18;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.МолочнаяПродукцияСерия6СимволовВЕТИС
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.МолочнаяПродукцияСерия6СимволовБезВЕТИС
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.МолочнаяПродукцияПодконтрольнаяВЕТИСГрупповаяУпаковка
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.МолочнаяПродукцияБезВЕТИСГрупповаяУпаковка
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.МолочнаяПродукцияПодконтрольнаяВЕТИСНабор
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.МолочнаяПродукцияБезВЕТИСНабор Тогда
		Результат = 20;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.НикотиносодержащаяПродукцияБлок
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.НикотиносодержащаяПродукцияНабор Тогда
		Результат = 21;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.НикотиносодержащаяПродукцияПачка Тогда
		Результат = 22;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.БАДы
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.БАДыГрупповаяУпаковка
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.БАДыНабор Тогда
		Результат = 23;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.Антисептики
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.АнтисептикиГрупповаяУпаковка
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.АнтисептикиНабор Тогда
		Результат = 25;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.МорепродуктыСтандартныйКМ
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.МорепродуктыГрупповаяУпаковкаСтандартныйКМ Тогда
		Результат = 27;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.БезалкогольноеПиво Тогда
		Результат = 28;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.СоковаяПродукция
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.СоковаяПродукцияНабор
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.СоковаяПродукцияГрупповаяУпаковка Тогда
		Результат = 29;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.БАДыСтандартныйКМ
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.БАДыГрупповаяУпаковкаСтандартныйКМ
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.БАДыНаборСтандартныйКМ Тогда
		Результат = 30;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.АнтисептикиСтандартныйКМ
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.АнтисептикиГрупповаяУпаковкаСтандартныйКМ
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.АнтисептикиНаборСтандартныйКМ Тогда
		Результат = 31;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.Морепродукты
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.МорепродуктыГрупповаяУпаковка Тогда
		Результат = 38;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.КормаДляЖивотныхСтандартныйКМ
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.КормаДляЖивотныхБезВЕТИССтандартныйКМ Тогда
		Результат = 26;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.Мясо Тогда
		Результат = 32;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ИгрыИИгрушкиДляДетей Тогда
		Результат = 34;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.РадиоэлектроннаяПродукцияСтандартныйКМ
			Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.РадиоэлектроннаяПродукцияГрупповаяУпаковкаСтандартныйКМ Тогда
		Результат = 36;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.РадиоэлектроннаяПродукция
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.РадиоэлектроннаяПродукцияГрупповаяУпаковка Тогда
		Результат = 37;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ТитановаяМеталлопродукция Тогда
		Результат = 39;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.РастительныеМасла Тогда
		Результат = 40;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.КормаДляЖивотных
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.КормаДляЖивотныхБезВЕТИС Тогда
		Результат = 41;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ОптоволокноИОптоволоконнаяПродукцияСтандартныйКМ
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ОптоволокноИОптоволоконнаяПродукцияГрупповаяУпаковкаСтандартныйКМ Тогда
		Результат = 44;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ОптоволокноИОптоволоконнаяПродукция
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ОптоволокноИОптоволоконнаяПродукцияГрупповаяУпаковка Тогда
		Результат = 45;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ПарфюмерныеИКосметическиеСредстваИБытоваяХимияСтандартныйКМ Тогда
		Результат = 46;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ПарфюмерныеИКосметическиеСредстваИБытоваяХимия Тогда
		Результат = 47;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.КонсервированнаяПродукцияСтандартныйКМ
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.КонсервированнаяПродукцияБезВЕТИССтандартныйКМ Тогда
		Результат = 48;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.КонсервированнаяПродукция
		Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.КонсервированнаяПродукцияБезВЕТИС Тогда
		Результат = 49;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ВетеринарныеПрепараты Тогда
		Результат = 50;
	ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.РастительныеМаслаСтандартныйКМ Тогда
		Результат = 51;
	Иначе
		ВызватьИсключение
			СтрШаблон(
				НСтр("ru = 'Неизвестный шаблон кода маркировки: %1'"),
				ЗначениеПоиска);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Перекодирует вид сервис провайдера для обмена с СУЗ ИС МП.
//
// Параметры:
//  ЗначениеПоиска - Неопределено - сервис-провайдер не используется, коды маркировки получаются самостоятельно,
//                   Строка, ПеречислениеСсылка.ВидыСервисПровайдеровСУЗ - значение для перекодировки.
// 
// Возвращаемое значение:
//  Строка, ПеречислениеСсылка.ВидыСервисПровайдеровСУЗ - Результат перекодирования.
//
Функция СпособИзготовленияКодовМаркировки(ЗначениеПоиска) Экспорт
	
	Если ЗначениеПоиска = Неопределено Тогда
		Результат = "SELF_MADE";
	Иначе
		
		Если ЗначениеПоиска = Перечисления.ВидыСервисПровайдеровСУЗ.ЦЭМ Тогда
			Результат =  "CEM";
		ИначеЕсли ЗначениеПоиска = Перечисления.ВидыСервисПровайдеровСУЗ.КонтрактноеПроизводство Тогда
			Результат =  "CM";
		ИначеЕсли ЗначениеПоиска = Перечисления.ВидыСервисПровайдеровСУЗ.ЛогистическийСклад Тогда
			Результат =  "CL";
		ИначеЕсли ЗначениеПоиска = Перечисления.ВидыСервисПровайдеровСУЗ.КомиссионнаяПлощадка Тогда
			Результат =  "CA";
		
		ИначеЕсли ЗначениеПоиска = "CEM" Тогда
			Результат = Перечисления.ВидыСервисПровайдеровСУЗ.ЦЭМ;
		ИначеЕсли ЗначениеПоиска = "CM" Тогда
			Результат = Перечисления.ВидыСервисПровайдеровСУЗ.КонтрактноеПроизводство;
		ИначеЕсли ЗначениеПоиска = "CL" Тогда
			Результат = Перечисления.ВидыСервисПровайдеровСУЗ.ЛогистическийСклад;
		ИначеЕсли ЗначениеПоиска = "CA" Тогда
			Результат = Перечисления.ВидыСервисПровайдеровСУЗ.КомиссионнаяПлощадка;
		Иначе
			ВызватьИсключение
				СтрШаблон(
					НСтр("ru = 'Неизвестный вид сервис-провайдера: %1'"),
					ЗначениеПоиска);
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Перекодирует способ формирования серийного номера кода маркировки для обмена с СУЗ ИС МП.
//
// Параметры:
//  ЗначениеПоиска - Строка, ПеречислениеСсылка.СпособыФормированияСерийногоНомераСУЗ - значение для перекодировки.
// 
// Возвращаемое значение:
//  Строка, ПеречислениеСсылка.СпособыФормированияСерийногоНомераСУЗ - Результат перекодирования.
//
Функция СпособФормированияСерийногоНомера(Знач ЗначениеПоиска) Экспорт
	
	Если ЗначениеПоиска = "SELF_MADE" Тогда
		Результат = Перечисления.СпособыФормированияСерийногоНомераСУЗ.Самостоятельно;
	ИначеЕсли ЗначениеПоиска = "OPERATOR" Тогда
		Результат = Перечисления.СпособыФормированияСерийногоНомераСУЗ.Автоматически;
	
	ИначеЕсли ЗначениеПоиска = Перечисления.СпособыФормированияСерийногоНомераСУЗ.Самостоятельно Тогда
		Результат =  "SELF_MADE";
	ИначеЕсли ЗначениеПоиска = Перечисления.СпособыФормированияСерийногоНомераСУЗ.Автоматически Тогда
		Результат =  "OPERATOR";
	Иначе
		ВызватьИсключение
			СтрШаблон(
				НСтр("ru = 'Неизвестный способ формирования серийного номера: %1'"),
				ЗначениеПоиска);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Параметры для настройки обмена с СУЗ
//
// Возвращаемое значение:
// 	Структура - Параметры для настройки обмена с СУЗ:
// * Таймаут - Число - Таймаут
// * Порт - Число - Порт
// * Адрес - Строка - Адрес
// * Идентификатор - Строка - Адрес
// * Токен - Строка - Адрес
// * ФорматОбмена - ПеречислениеСсылка.ВерсииФорматаОбменаСУЗ - Формат обмена с СУЗ
Функция НоваяНастройкаОбменаСУЗ() Экспорт
	
	НастройкиОбмена = Новый Структура;
	НастройкиОбмена.Вставить("Сервер",                           "suz2.crpt.ru");
	НастройкиОбмена.Вставить("Порт",                             443);
	НастройкиОбмена.Вставить("Таймаут",                          60);
	НастройкиОбмена.Вставить("СтанцияУправленияЗаказами",        Справочники.СтанцииУправленияЗаказамиИСМП.ПустаяСсылка());
	НастройкиОбмена.Вставить("Идентификатор",                    "");
	НастройкиОбмена.Вставить("Токен",                            "");
	НастройкиОбмена.Вставить("ИмяПользователя",                  "");
	НастройкиОбмена.Вставить("ФорматОбмена",                     Неопределено);
	НастройкиОбмена.Вставить("ИспользоватьЗащищенноеСоединение", Ложь);
	НастройкиОбмена.Вставить("ПредставлениеСервиса",             НСтр("ru = 'СУЗ'"));
	НастройкиОбмена.Вставить("ДатаПоследнегоЗапроса",            '00010101');
	НастройкиОбмена.Вставить("ИнтервалМеждуЗапросами",           1);
	НастройкиОбмена.Вставить("ФоновоеЗадание",                   Неопределено);
	НастройкиОбмена.Вставить("ПолучатьКодовЗаИтерацию",          1000);
	НастройкиОбмена.Вставить("Организация",                      Неопределено);
	НастройкиОбмена.Вставить("ИдентификаторСоединения",          "");
	НастройкиОбмена.Вставить("ПроизводственныйОбъект",           Неопределено);
	НастройкиОбмена.Вставить("ПроизводственныйОбъектАдрес",      "");
	НастройкиОбмена.Вставить("КодФИАС",                          "");
	
	Возврат НастройкиОбмена;
	
КонецФункции

// Перекодирует целевой пол при получении новых GTIN маркировки остатков для обмена с СУЗ ИС МП.
//
// Параметры:
//  ЗначениеПоиска - Строка, ПеречислениеСсылка.ЦелевойПолИСМП - значение для перекодировки.
// 
// Возвращаемое значение:
//  Строка, ПеречислениеСсылка.ЦелевойПолИСМП - Результат перекодирования.
//
Функция ЦелевойПол(Знач ЗначениеПоиска) Экспорт
	
	Если ВРег(ЗначениеПоиска) = "MALE" Тогда
		Результат = Перечисления.ЦелевойПолИСМП.Мужская;
	ИначеЕсли ВРег(ЗначениеПоиска) = "FEMALE" Тогда
		Результат = Перечисления.ЦелевойПолИСМП.Женская;
	ИначеЕсли ВРег(ЗначениеПоиска) = "BABY" Тогда
		Результат = Перечисления.ЦелевойПолИСМП.Детская;
	ИначеЕсли ВРег(ЗначениеПоиска) = "UNISEX" Тогда
		Результат = Перечисления.ЦелевойПолИСМП.Унисекс;
		
	ИначеЕсли ЗначениеПоиска = Перечисления.ЦелевойПолИСМП.Унисекс Тогда
		Результат = "UNISEX";
	ИначеЕсли ЗначениеПоиска = Перечисления.ЦелевойПолИСМП.Детская Тогда
		Результат = "BABY";
	ИначеЕсли ЗначениеПоиска = Перечисления.ЦелевойПолИСМП.Женская Тогда
		Результат = "FEMALE";
	ИначеЕсли ЗначениеПоиска = Перечисления.ЦелевойПолИСМП.Мужская Тогда
		Результат = "MALE";
	Иначе
		ВызватьИсключение
			СтрШаблон(
				НСтр("ru = 'Неизвестный целевой пол: %1'"),
				ЗначениеПоиска);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Перекодирует возрастную категорию при получении новых GTIN маркировки остатков для обмена с СУЗ ИС МП.
//
// Параметры:
//  ЗначениеПоиска - Строка, ПеречислениеСсылка.ВозрастныеКатегорииИСМП - значение для перекодировки.
// 
// Возвращаемое значение:
//  Строка, ПеречислениеСсылка.ВозрастныеКатегорииИСМП - Результат перекодирования.
//
Функция ВозрастнаяКатегория(Знач ЗначениеПоиска) Экспорт
	
	Если ВРег(ЗначениеПоиска) = "ADULT" Тогда
		Результат = Перечисления.ВозрастныеКатегорииИСМП.Взрослое;
	ИначеЕсли ВРег(ЗначениеПоиска) = "BABY" Тогда
		Результат = Перечисления.ВозрастныеКатегорииИСМП.Детское;
	ИначеЕсли ВРег(ЗначениеПоиска) = "NO_AGE" Тогда
		Результат = Перечисления.ВозрастныеКатегорииИСМП.БезВозраста;
		
	ИначеЕсли ЗначениеПоиска = Перечисления.ВозрастныеКатегорииИСМП.Взрослое Тогда
		Результат = "ADULT";
	ИначеЕсли ЗначениеПоиска = Перечисления.ВозрастныеКатегорииИСМП.Детское Тогда
		Результат = "BABY";
	ИначеЕсли ЗначениеПоиска = Перечисления.ВозрастныеКатегорииИСМП.БезВозраста Тогда
		Результат = "NO_AGE";
	Иначе
		ВызватьИсключение
			СтрШаблон(
				НСтр("ru = 'Неизвестнав возрастная категория: %1'"),
				ЗначениеПоиска);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Перекодирует шаблон кода маркировки и тип кода для обмена с СУЗ ИС МП.
//
// Параметры:
//  ЗначениеПоиска - Строка, ПеречислениеСсылка.ШаблоныКодовМаркировкиСУЗ - значение для перекодировки.
// 
// Возвращаемое значение:
//  Строка, ПеречислениеСсылка.ШаблоныКодовМаркировкиСУЗ - Результат перекодирования.
//
Функция ТипКодаМаркировки(Знач ЗначениеПоиска) Экспорт
	
	Если ТипЗнч(ЗначениеПоиска) = Тип("ПеречислениеСсылка.ШаблоныКодовМаркировкиСУЗ") Тогда
		
		ВидУпаковки = ИнтеграцияИСМПСлужебныйКлиентСервер.ВидУпаковкиПоШаблонуКодаМаркировки(ЗначениеПоиска);
		Если ВидУпаковки = Перечисления.ВидыУпаковокИС.Групповая Тогда
			Результат = "GROUP";
		ИначеЕсли ВидУпаковки = Перечисления.ВидыУпаковокИС.Набор Тогда
			Результат = "SET";
		ИначеЕсли ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ДухиКомплект
			Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ФотоаппаратыКомплект
			Или ЗначениеПоиска = Перечисления.ШаблоныКодовМаркировкиСУЗ.ЛегкаяПромышленностьКомплект Тогда
			Результат = "BUNDLE";
		Иначе
			Результат = "UNIT";
		КонецЕсли;
	
	ИначеЕсли ЗначениеПоиска = "UNIT" Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.Духи;
	ИначеЕсли ЗначениеПоиска = "BUNDLE" Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.ДухиКомплект;
	ИначеЕсли ЗначениеПоиска = "GROUP" Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.АнтисептикиГрупповаяУпаковка;
	ИначеЕсли ЗначениеПоиска = "SET" Тогда
		Результат = Перечисления.ШаблоныКодовМаркировкиСУЗ.АнтисептикиНабор;
	
	Иначе
		ВызватьИсключение
			СтрШаблон(
				НСтр("ru = 'Неизвестный тип кода маркировки: %1'"),
				ЗначениеПоиска);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Перекодирует тип оплаты кодов маркировки для обмена с СУЗ ИС МП.
//
// Параметры:
//  ЗначениеПоиска - Строка, ПеречислениеСсылка.ТипыОплатыКодовМаркировкиСУЗ - значение для перекодировки.
// 
// Возвращаемое значение:
//  Строка, ПеречислениеСсылка.ТипыОплатыКодовМаркировкиСУЗ - Результат перекодирования.
//
Функция ТипОплатыКодовМаркировки(Знач ЗначениеПоиска) Экспорт
	
	Если ЗначениеПоиска = Перечисления.ТипыОплатыКодовМаркировкиСУЗ.ОплатаПоЭмиссии Тогда
		Результат = 1;
	ИначеЕсли ЗначениеПоиска = Перечисления.ТипыОплатыКодовМаркировкиСУЗ.ОплатаПоНанесению Тогда
		Результат = 2;
	
	ИначеЕсли ЗначениеПоиска = 1 Тогда
		Результат = Перечисления.ТипыОплатыКодовМаркировкиСУЗ.ОплатаПоЭмиссии;
	ИначеЕсли ЗначениеПоиска = 2 Тогда
		Результат = Перечисления.ТипыОплатыКодовМаркировкиСУЗ.ОплатаПоНанесению;
	Иначе
		ВызватьИсключение
			СтрШаблон(
				НСтр("ru = 'Неизвестный тип оплаты кодов маркировки СУЗ: %1'"),
				ЗначениеПоиска);
	КонецЕсли;

	Возврат Результат;

КонецФункции

// Преобразует значение перечисления в код, который соответствует формату обмена с ИСМП.
//
// Параметры:
//  ЗначениеПоиска - Строка, ПеречислениеСсылка.СпособыПолученияИзготовленныхКИЗГИСМ - преобразуемое значение.
//
// Возвращаемое значение:
//  Строка, ПеречислениеСсылка.СпособыПолученияИзготовленныхКИЗГИСМ - Перекодированное значение.
//
Функция СпособПолученияИзготовленныхКИЗ(Знач ЗначениеПоиска) Экспорт

	Если ЗначениеПоиска = Перечисления.СпособыПолученияИзготовленныхКИЗГИСМ.СамостоятельноеПолучениеУЭмитента Тогда
		Результат = "SELF_OBTAIN";
	ИначеЕсли ЗначениеПоиска = Перечисления.СпособыПолученияИзготовленныхКИЗГИСМ.ДоставкаЭмитентомДоУказанногоМеста Тогда
		Результат = "DELIVERY";
	ИначеЕсли ЗначениеПоиска = Перечисления.СпособыПолученияИзготовленныхКИЗГИСМ.ЧерезМногофункциональныйЦентр Тогда
		Результат = "MFC";

	ИначеЕсли ЗначениеПоиска = "SELF_OBTAIN" Тогда
		Результат = Перечисления.СпособыПолученияИзготовленныхКИЗГИСМ.СамостоятельноеПолучениеУЭмитента;
	ИначеЕсли ЗначениеПоиска = "DELIVERY" Тогда
		Результат = Перечисления.СпособыПолученияИзготовленныхКИЗГИСМ.ДоставкаЭмитентомДоУказанногоМеста;
	ИначеЕсли ЗначениеПоиска = "MFC" Тогда
		Результат = Перечисления.СпособыПолученияИзготовленныхКИЗГИСМ.ЧерезМногофункциональныйЦентр;
	Иначе
		ВызватьИсключение
			СтрШаблон(
				НСтр("ru = 'Неизвестный способ получения изготовленных КИЗ: %1'"),
				ЗначениеПоиска);
	КонецЕсли;

	Возврат Результат;

КонецФункции

// Преобразует значение перечисления в код, который соответствует формату обмена с ИСМП.
//
// Параметры:
//  ЗначениеПоиска - Строка, ПеречислениеСсылка.ВидыКиЗГИСМ - преобразуемое значение.
//
// Возвращаемое значение:
//  Строка, ПеречислениеСсылка.ВидыКиЗГИСМ  - Перекодированное значение.
//
Функция ВидКИЗ(Знач ЗначениеПоиска) Экспорт

	Если ЗначениеПоиска = Перечисления.ВидыКиЗГИСМ.Вшивной Тогда
		Результат = "SEWN";
	ИначеЕсли ЗначениеПоиска = Перечисления.ВидыКиЗГИСМ.Клеевой Тогда
		Результат = "GLUED";
	ИначеЕсли ЗначениеПоиска = Перечисления.ВидыКиЗГИСМ.Навесной Тогда
		Результат = "HINGED";

	ИначеЕсли ЗначениеПоиска = "SEWN" Тогда
		Результат = Перечисления.ВидыКиЗГИСМ.Вшивной;
	ИначеЕсли ЗначениеПоиска = "GLUED" Тогда
		Результат = Перечисления.ВидыКиЗГИСМ.Клеевой;
	ИначеЕсли ЗначениеПоиска = "HINGED" Тогда
		Результат = Перечисления.ВидыКиЗГИСМ.Навесной;
	Иначе
		ВызватьИсключение
			СтрШаблон(
				НСтр("ru = 'Неизвестный вид КИЗ: %1'"),
				ЗначениеПоиска);
	КонецЕсли;

	Возврат Результат;

КонецФункции

// Преобразует значение перечисления в код, который соответствует формату обмена с ИСМП.
//
// Параметры:
//  ЗначениеПоиска - Строка, ПеречислениеСсылка.РазмерыКиЗГИСМ - преобразуемое значение.
//
// Возвращаемое значение:
//  Строка, ПеречислениеСсылка.РазмерыКиЗГИСМ  - Перекодированное значение.
//
Функция РазмерКИЗ(Знач ЗначениеПоиска) Экспорт

	Если ЗначениеПоиска = Перечисления.РазмерыКиЗГИСМ.Размер25x160 Тогда
		Результат = "SMALL";
	ИначеЕсли ЗначениеПоиска = Перечисления.РазмерыКиЗГИСМ.Размер53x80 Тогда
		Результат = "BIG";

	ИначеЕсли ЗначениеПоиска = "SMALL" Тогда
		Результат = Перечисления.РазмерыКиЗГИСМ.Размер25x160;
	ИначеЕсли ЗначениеПоиска = "BIG" Тогда
		Результат = Перечисления.РазмерыКиЗГИСМ.Размер53x80;
	Иначе
		ВызватьИсключение
			СтрШаблон(
				НСтр("ru = 'Неизвестный размер КИЗ: %1'"),
				ЗначениеПоиска);
	КонецЕсли;

	Возврат Результат;
	
КонецФункции

Функция СтранаЭмиссииПоСерийномуНомеру(Знач КодСтраны) Экспорт
	
	Если КодСтраны = "1" Тогда
		Возврат Неопределено; // Республика Армения
	ИначеЕсли КодСтраны = "2" Тогда
		Возврат "112"; // Республика Беларусь
	ИначеЕсли КодСтраны = "3" Тогда
		Возврат "398"; // Республика Казахстан
	ИначеЕсли КодСтраны = "4" Тогда
		Возврат Неопределено; // Киргизская Республика
	ИначеЕсли КодСтраны = "5" Тогда
		Возврат Неопределено; // Российская Федерация
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

#КонецОбласти
